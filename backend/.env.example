APP_NAME="Digital Courses"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database
# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
#LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=db
DB_PORT=5432
DB_DATABASE=digital_courses
DB_USERNAME=laravel
DB_PASSWORD=secret

POSTGRES_DB=digital_courses
POSTGRES_USER=laravel
POSTGRES_PASSWORD=secret


# PostgreSQL (variaveis para o container)
POSTGRES_DB=digital_courses
POSTGRES_USER=laravel
POSTGRES_PASSWORD=secret

# pgAdmin (UI)
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin

BROADCAST_DRIVER=log
CACHE_DRIVER=redis
FILESYSTEM_DISK=s3
QUEUE_CONNECTION=sync
SESSION_DRIVER=redis
SESSION_LIFETIME=120

REDIS_PORT=6379
REDIS_HOST=redis
REDIS_PASSWORD=null

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=digital-courses-bucket
AWS_USE_PATH_STYLE_ENDPOINT=true
AWS_ENDPOINT=http://minio:9000

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1


PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"

# -----------------------------------------------
# Swagger / OpenAPI (L5-Swagger)
# -----------------------------------------------

# DEV: gera automaticamente a cada requisição. Em produção, manter false.
L5_SWAGGER_GENERATE_ALWAYS=true
L5_SWAGGER_CONST_HOST=http://localhost:8000
L5_SWAGGER_BASE_PATH=/api/v1
L5_SWAGGER_USE_ABSOLUTE_PATH=true

# SEGURANÇA: controle de acesso ao Swagger UI
# - local/testing: sempre acessível (middleware ignora)
# - staging:       acessível com Basic Auth (definir USER e PASSWORD abaixo)
# - production:    bloqueado (404) por padrão. Só libera com SWAGGER_ENABLED=true
SWAGGER_ENABLED=true
SWAGGER_USER=admin
SWAGGER_PASSWORD=troque-esta-senha-antes-de-subir
